---
import PlatformIcon from './PlatformIcon.astro';
import type { Platform } from '../types/platform';

const platforms = [
  {
    label: 'macOS',
    platform: 'macOS',
    href: 'https://github.com/crushdotsoy/fastrenamer/releases/latest/download/Fast.Renamer-latest-arm64.dmg',
  },
  {
    label: 'Windows',
    platform: 'Windows',
    href: 'https://github.com/crushdotsoy/fastrenamer/releases/latest/download/Fast.Renamer-latest-x64.exe',
  },
  {
    label: 'Linux',
    platform: 'Linux',
    href: 'https://github.com/crushdotsoy/fastrenamer/releases/latest/download/Fast.Renamer-latest.AppImage',
  },
] satisfies { label: string; platform: Platform; href: string }[];
---

<div class="flex flex-col sm:flex-row gap-4 justify-center flex-wrap">
  {platforms.map((p, index) => (
    <a
      href={p.href}
      target="_blank"
      rel="noopener noreferrer"
      aria-label={`Download Fast Renamer for ${p.label}`}
      data-reveal="up"
      style={`--reveal-delay: ${index * 80}ms;`}
      class="flex items-center gap-3 bg-[#FEFCFF] border-2 border-[#C8C5F5] text-[#2D2640] px-6 py-4 rounded-2xl font-semibold hover:border-[#F4A0B5] hover:scale-105 hover:shadow-pink transition-all duration-200 min-w-[180px] justify-center"
    >
      <PlatformIcon platform={p.platform} class="h-7 w-7 shrink-0" />
      <span class="font-heading font-bold text-base">{p.label}</span>
    </a>
  ))}
</div>
